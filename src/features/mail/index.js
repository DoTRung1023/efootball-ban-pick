/** Mail feature — the one SMTP transport and the messages the app sends. */
export { mailConfigured, sendMail } from "./transport.js";
export { newPasswordEmail, verificationEmail } from "./templates.js";
